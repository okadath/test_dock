# version: '3.7'

# services:
#   web:
#     build: ./
#     command: gunicorn OnlineEXP.wsgi:application --bind 0.0.0.0:8000
#     ports:
#       - 8000:80
#     # con esto exponemos el puerto a nginx, sustituyendo lo de arriba
#     # expose:
#     #   - 8000
#   nginx:
#     restart: always
#     build: ./nginx
#     ports:
#       - "8000:80"
#     depends_on:
#       - web

version: '3'

services:
  web:
    restart: always
    build: ./
    # ports:
    #   - "8000:8000"
    expose:
      - "8000"
    # links:
    #   - postgres:postgres
    #   - redis:redis
    # volumes:
    #   - web-django:/usr/src/app
    #   - web-static:/usr/src/app/static
    # env_file: .env
    # environment:
    #   DEBUG: 'true'
    command: gunicorn OnlineEXP.wsgi:application -w 2  --bind 0.0.0.0:8000

  nginx:
    restart: always
    build: ./nginx
    ports:
      - "80:80"
    depends_on:
      - web
    # network_mode: host
    # volumes:
    #   - web-static:/www/static
    # links:
    #   - web:web

  # postgres:
  #   restart: always
  #   image: postgres:latest
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data/

  # redis:
  #   restart: always
  #   image: redis:latest
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redisdata:/data

# volumes:
#   web-django:
#   web-static:
#   pgdata:
#   redisdata:
