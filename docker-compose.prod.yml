version: '3.7'

services:
  web:
    build: ./
    command: gunicorn OnlineEXP.wsgi:application --bind 0.0.0.0:8000
    # ports:
    #   - 8000:8000
    # con esto exponemos el puerto a nginx, sustituyendo lo de arriba
    expose:
      - 8000
    # env_file:
    #   - ./.env.prod
#     depends_on:
#       - db
#   db:
#     image: postgres:12.0-alpine
#     volumes:
#       - postgres_data:/var/lib/postgresql/data/
#     env_file:
#       - ./.env.prod.db

# volumes:
#   postgres_data:
  nginx:
    build: ./nginx
    ports:
      - 1337:80
    depends_on:
      - web